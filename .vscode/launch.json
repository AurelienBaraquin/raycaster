{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Debug raycaster-native",
      "cargo": {
        "args": ["build", "--bin=raycaster-native", "--package=raycaster-native"]
      },
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
